@using WebAppExam.Models;
@model SubModel

<section class="sub">
    <div class="container-fluid" style="background-image: url(@Model.BackgroundImg)">
        <div class="shop-heading">
            @Model.Heading<br><span>@Model.Subheading</span>
        </div>
    </div>
</section>
